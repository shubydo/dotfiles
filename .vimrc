set nocompatible " require vi compatibility mode is not used

" Settings
syntax on
set number
set noswapfile " disable swap file
set hlsearch " highlight search results
set ignorecase " ignore case in search
set incsearch " show search results while typing

" Remap escape key
" inoremap jk <ESC>
" inoremap jj <ESC>

" Change leader key
" let mapleader = "'"
